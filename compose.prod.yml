services:
  gui:
    restart: unless-stopped
    build:
      args:
        - CONFIGURATION=production

  mongo:
    restart: unless-stopped

  server:
    restart: unless-stopped
    environment:
      - GIN_MODE=release

  report:
    restart: unless-stopped
    environment:
      - GIN_MODE=release
