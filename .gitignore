# whitelist init (prohibit everything, allow all directories)
*
!*/

# prohibit directories that can't be accessed with user rights (removes warnings)
/archive
/server/database/data

# allow visual studio code config
!.vscode/settings.json
!.vscode/extensions.json

# allow gitginore files
!.gitignore

# allow project readme
!/README.md

# allow environment file
!/.env.example

# allow docker compose files
!/compose.yml
!/compose.*.yml

# allow go workspace files
!/go.work
!/go.work.sum